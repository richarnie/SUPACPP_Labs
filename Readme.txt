type make into the terminal to compile and create executable
type in the terminal ./AnalyseData to run the program
The path to the file is from the file set up used when creating the file so if the text file is not found the path needs to be changed